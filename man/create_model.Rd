% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.r
\name{create_model}
\alias{create_model}
\title{Create a model from a Modelfile}
\usage{
create_model(
  model,
  from = NULL,
  template = NULL,
  license = NULL,
  system = NULL,
  parameters = NULL,
  messages = NULL,
  quantize = NULL,
  stream = TRUE,
  ...,
  server = NULL,
  verbose = getOption("rollama_verbose", default = interactive())
)
}
\arguments{
\item{model}{name of the model to create}

\item{from}{existing model to create from}

\item{template}{prompt template to use for the model}

\item{license}{license string or list of licenses for the model}

\item{system}{system prompt to embed in the model}

\item{parameters}{key-value parameters for the model}

\item{messages}{message history to use for the model (array of ChatMessage objects)}

\item{quantize}{quantization level to apply (e.g. \code{"q4_K_M"}, \code{"q8_0"})}

\item{stream}{stream status updates (default: \code{TRUE})}

\item{...}{additional arguments (currently unused)}

\item{server}{URL to one or several Ollama servers (not the API). Defaults to
"http://localhost:11434".}

\item{verbose}{Whether to print status messages to the Console. Either
\code{TRUE}/\code{FALSE} or see \link[httr2:progress_bars]{httr2::progress_bars}. The default is to have status
messages in interactive sessions. Can be changed with
\code{options(rollama_verbose = FALSE)}.}
}
\value{
(invisible) a tibble with information about the created model
}
\description{
Create a model from a Modelfile
}
\details{
Custom models are the way to save your system message and model
parameters in a dedicated shareable way. If you use \code{show_model()}, you can
look at the configuration of a model in the column modelfile. To get more
information and a list of valid parameters, check out
\url{https://github.com/ollama/ollama/blob/main/docs/modelfile.md}. Most
options are also available through the \code{query} and \code{chat} functions, yet
are not persistent over sessions.
}
\examples{
\dontshow{if (ping_ollama(silent = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
create_model("mario", from = "llama3.1", system = "You are mario from Super Mario Bros.")
\dontshow{\}) # examplesIf}
}
